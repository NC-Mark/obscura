# PII Shield v7 — full dependency list
# Run: pip install -r requirements.txt
# Then: python -m spacy download en_core_web_sm

# ── MCP (Claude Desktop integration) ──────────────────────────────────────────
mcp[cli]>=1.0.0

# ── NLP / NER ─────────────────────────────────────────────────────────────────
presidio-analyzer>=2.2.355
spacy>=3.7.0
# After install run: python -m spacy download en_core_web_sm
# torch is installed separately by setup.py (CPU-only build)
# Do NOT add torch here — PyPI's default Windows build includes CUDA and will
# fail on machines without CUDA drivers. setup.py uses the CPU wheel index.
gliner>=0.2.7

# ── Document handling ─────────────────────────────────────────────────────────
python-docx>=1.1.0
pdfplumber>=0.10.0
lxml>=5.0.0

# ── REST API (standalone server) ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0
cryptography>=42.0.0
python-stdnum>=1.19
